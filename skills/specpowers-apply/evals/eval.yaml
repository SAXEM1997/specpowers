schema_version: v1alpha1

environment:
  type: none

skills:
  - source: local_path
    path: .
  - source: local_path
    path: ../specpowers

engine:
  name: claude_code

cases:
  files:
    - evals/cases/subagent-requirement.yaml
    - evals/cases/inline-degradation.yaml
  defaults:
    timeout_seconds: 300
    max_turns: 8

report:
  formats: [json, html]
